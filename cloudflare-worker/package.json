{
  "name": "torbox-proxy-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker to proxy TorBox API and bypass CORS",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
